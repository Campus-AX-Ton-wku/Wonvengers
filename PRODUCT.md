# PRODUCT — Perky (퍼키)

디자인 작업이 매번 다시 물어보지 않아도 되도록, 바뀌지 않는 제품 맥락만 적는다.
기능 명세는 `docs/기획/PRD.md` 가 단독으로 소유한다. 여기서는 중복하지 않는다.

## 한 줄

> 받을 수 있는 주거 지원금이 뭐가 있고, 그걸 받으면 내가 실제로 얼마를 내게 될까?

## 무엇인가

전북·익산 청년의 주거 지원금 **발견 + 실부담 계산기**. 두 층으로 동작한다.

- **1층 (발견)** — 질문 4개(생년월일·지역·현재 상태·소득 구간)에 답하면 흩어져
  있는 지원금을 한 목록으로 모아 보여준다. "몰라서 못 받는 문제" 를 푼다.
- **2층 (정밀 계산)** — 계약 조건을 넣으면 최대 지원 가능액과 최종 예상 주거비를
  계산한다. "받으면 얼마나 도움이 되는지 모르는 문제" 를 푼다.

1층만으로도 제품으로 성립한다.

## 누가 쓰나

전북특별자치도, 특히 익산시에 살거나 이사할 예정인 **만 18~39세 청년**. 원광대
재학생·졸업생이 1차 대상이다.

쓰는 상황이 설계를 좌우한다 — **방을 알아보는 중이거나 막 계약을 앞둔 시점에,
모바일에서, 처음이자 아마도 유일하게** 들어온다. 반복 사용자가 아니다. 학습을
요구하는 UI 는 실패한다.

## 성공은 무엇인가

사용자가 **자기가 대상인 지원금을 하나라도 발견하고 공식 신청 페이지까지 닿는 것**.
앱 안에서 신청이 완료되지 않는다. 앱의 역할은 여기까지다.

## 이 제품만의 진실

**정확성이 기능보다 앞선다.** 이 앱은 돈에 관한 판단을 돕는다. 틀린 안내는 없는
안내보다 나쁘다. 그래서 지켜 온 규칙들이 코드 곳곳에 주석으로 남아 있다.

- **모르면 모른다고 말한다.** 판단할 수 없을 때 숫자를 지어내지 않는다.
  `'확인 필요'` 태그와 `null`(모름) 이 1급 상태다
- **검수 여부를 밝힌다.** 팀이 공고 원문과 대조한 정책은 대조 날짜를, 아직 안 한
  정책은 안 했다고 카드에 적는다
- **출처를 항상 붙인다.** 카드마다 공고 원문 링크가 있다. 숫자가 어디서 왔는지
  물을 수 있어야 한다
- **자격을 확정하지 않는다.** 최종 판단은 각 기관이 심사한다는 고지를 접지 않는다
- **답변은 브라우저에만 둔다.** 서버로 보내지 않는다 (PRD F0-13)

**데이터는 사람이 옮긴 값이다.** 정책 5건은 팀이 공고를 손으로 옮겼다. 자동
수집이 아니므로 규모가 아니라 정확성이 경쟁력이다.

## 기술 제약

- Next.js **정적 export**. 서버가 없다 — 오늘 날짜조차 브라우저에서 채워야 한다
  (빌드 시점 날짜가 HTML 에 박히면 안 된다)
- 저장소는 `localStorage` 뿐. 계정도 동기화도 없다
- 배포는 Vercel, Root Directory 는 `apps/web`
- 모바일 우선. 실사용은 사실상 전부 폰이다

## 디자인 세계

따뜻하고 조용한 팔레트를 쓴다. 돈 이야기를 하는 화면이라 들뜨면 신뢰를 잃는다.
토큰은 `apps/web/app/globals.css` 가 단독 출처다 (`docs/디자인/design-tokens.md`).

| 역할 | 값 |
| --- | --- |
| 페이지 배경 | sand `#f5efeb` |
| 본문·제목 | ink `#2f4156` ~ `#5b6f86` |
| 기본 액션 | brand slate blue `#567c8d` |
| 금액 | accent terracotta `#a85434` |
| 주의 | warn `#7a5518` |

**이 세계를 다른 앱의 팔레트로 대체하지 않는다.** 레퍼런스에서 배우는 것은
구조·밀도·타이포 위계이지 색이 아니다.

## 톤

존댓말, 짧은 문장. 사용자를 재촉하거나 과장하지 않는다. 나쁜 소식(대상 아님,
접수 마감)도 숨기지 않고 그다음에 무엇을 할 수 있는지 말한다.

## 팀

Wonvengers · 원광대학교 · 멋쟁이사자처럼 Campus AX-Ton.
