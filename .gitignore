node_modules/
.next/
out/
build/
*.log
.env*
.DS_Store
next-env.d.ts
*.tsbuildinfo

# 디자인 레퍼런스 캡처 (타사 앱 화면 — 로컬에만 보관)
docs/디자인/reference/

# 개인 도구 설정 — 저장소에 올리지 않는다
# .claude/settings.local.json 은 절대 경로가 하드코딩돼 있어 다른 PC 에서는 동작하지 않는다
.claude/
# impeccable 디자인 진단 스냅샷 (로컬 기록)
.impeccable/

# 루트에 굴러다니는 로고 원본 — 정식 마스터는 docs/디자인/아이콘/ 에 있다
/wonvengers-logo.jpg

# 신선도 감지 산출물 — 보고서는 매 실행마다 새로 만들고,
# 스냅샷은 보호 브랜치 대신 freshness-snapshot 브랜치에 둔다
# (develop·main 은 PR 필수라 워크플로가 직접 푸시할 수 없다)
apps/web/freshness-report.md
apps/web/scripts/freshness/freshness-snapshot.json
